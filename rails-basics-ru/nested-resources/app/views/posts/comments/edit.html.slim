= content_for :title do
  = 'Editing comment'

/ = debugger

/ == render 'form', comment: @comment
/ = debugger
== render 'form', comment: @comment
/ = debugger

hr

.d-flex
  = link_to 'Back', :back, class: 'btn btn-outline-primary'
